import type { CctvCamera, IncidentReport } from '../types/cctv';

// Default empty datasets - Only real cameras and incidents from Supabase / local storage are displayed
export const INITIAL_CAMERAS: CctvCamera[] = [];

export const INITIAL_INCIDENTS: IncidentReport[] = [];
